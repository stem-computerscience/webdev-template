# Div and Flex

**Type:** BubbleChoice

*Find an extra challenge or a new type of code here.*

## Options

- **a.** Flex-flow — see `Option a - Flex-flow/`
- **b.** Gap spacing — see `Option b - Gap spacing/`
- **c.** Child Selectors — see `Option c - Child Selectors/`
- **d.** Descendant Selectors — see `Option d - Descendant Selectors/`
- **e.** Image Gallery Cards — see `Option e - Image Gallery Cards/`
- **f.** Add to your Web Page! — see `Option f - Add to your Web Page!/`
