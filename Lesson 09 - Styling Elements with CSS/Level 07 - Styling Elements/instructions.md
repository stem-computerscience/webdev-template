# Styling Elements

**Type:** BubbleChoice

*Practice your new skills here.*

## Options

- **a.** Debug some images — see `Option a - Debug some images/`
- **b.** Debug the style — see `Option b - Debug the style/`
- **c.** Debug the Heading — see `Option c - Debug the Heading/`
- **d.** Add New Rules — see `Option d - Add New Rules/`
- **e.** Fix Image Width — see `Option e - Fix Image Width/`
- **f.** Fix Image Position — see `Option f - Fix Image Position/`
