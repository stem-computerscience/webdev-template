# Level 2: Display, Direction and Wrap

**Lesson:** [Lesson 12 — Organizing Content with Flexbox](../README.md)
**Type:** Skill Building
**Source:** https://studio.code.org/courses/ai-discoveries-2026/units/3/lessons/12/levels/2

## Instructions

The first three steps we typically follow when applying Flex properties are: 1) establish the layout, 2) define the flow direction and if items should wrap, and 3) define how the items should be distributed. Let's try out the first two steps: 1) establish flex layout and 2) define the direction and wrap.

### Do This
- Add a div container around the image tags.
- Add the class name `.container` to the div tag.
- Add the following Flex properties to the container:
  - Establish flex layout with `display: flex;`
  - Define the flow direction with `flex-direction: row;`
  - Define if items should wrap with `flex-wrap: wrap;`
- Change the size of your Preview window to see the wrapping effects (see the gif provided on the level page).
- Change the values for the `flex-direction` property to see what each one does. Value options include:
  - `row`
  - `row-reverse`
  - `column`
  - `column-reverse`
- Change the values for the `flex-wrap` property to see what each one does. Value options include:
  - `wrap`
  - `nowrap`
  - `wrap-reverse`

*Guided walkthroughs available: "Show Me How To Complete This Level" and "Show Me How To Resize The Preview Window." Don't forget to refer back to your Flexbox Cheat Sheet when needed!*

**Files:** `index.html`, `style.css`, `one.jpg` – `six.jpg`
