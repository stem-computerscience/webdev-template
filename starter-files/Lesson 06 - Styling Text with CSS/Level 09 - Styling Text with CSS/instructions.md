# Styling Text with CSS

**Type:** BubbleChoice

*Try out your new skills on these different challenges!*

## Options

- **a.** Debug the font size — see `Option a - Debug the font size/`
- **b.** Debug missing paragraph — see `Option b - Debug missing paragraph/`
- **c.** Debug missing heading css — see `Option c - Debug missing heading css/`
- **d.** Debug the RGB color style — see `Option d - Debug the RGB color style/`
- **e.** Debug the missing styles — see `Option e - Debug the missing styles/`
- **f.** Add a heading rule-set — see `Option f - Add a heading rule-set/`
- **g.** Add a paragraph rule-set — see `Option g - Add a paragraph rule-set/`
- **h.** Add a style sheet — see `Option h - Add a style sheet/`
