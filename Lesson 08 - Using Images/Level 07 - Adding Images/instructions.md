# Adding Images

**Type:** BubbleChoice

*Practice your new skills here.*

## Options

- **a.** Image Bug Match — see `Option a - Image Bug Match/`
- **b.** Debug the missing image — see `Option b - Debug the missing image/`
- **c.** Debug the missing image 2 — see `Option c - Debug the missing image 2/`
- **d.** Debug the missing image 3 — see `Option d - Debug the missing image 3/`
- **e.** Add alt to existing image — see `Option e - Add alt to existing image/`
- **f.** Add image link — see `Option f - Add image link/`
- **g.** Add new image — see `Option g - Add new image/`
